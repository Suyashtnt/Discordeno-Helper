{
	"$schema": "https://deno.land/x/denon/schema.json",
	"scripts": {
		"start": {
			"cmd": "deno run --allow-net --allow-write --allow-read --allow-plugin --unstable --config=tsconfig.json tests/mod.ts",
			"desc": "run my app.ts file"
		}
	},
	"watcher": {
		"match": ["**/*"]
	}
}
