{
  "$schema": "https://deno.land/x/denon/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-read tests/mod.ts",
      "desc": "run my app.ts file"
    }
  },
  "watcher": {
    "match": ["**/*"]
  }
}
